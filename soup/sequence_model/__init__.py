"""Sequence-model runtime: shard-loaded Qwen/Llama layers and the boundary wire.

Kept intentionally empty so importing a submodule (e.g. the worker) does not
eagerly load anything else — import the specific modules you need directly.
"""
