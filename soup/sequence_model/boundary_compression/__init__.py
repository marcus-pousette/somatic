"""Boundary wire codecs for distributed sequence-model splits.

Import the codec modules directly (e.g. ``wire_codec_runtime``,
``trainable_autoencoder_wire``); this package init is intentionally empty so
importing one codec does not pull the others.
"""
